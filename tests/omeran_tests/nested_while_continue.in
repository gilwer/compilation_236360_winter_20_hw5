void main() {
    int i = 1;
    int j = 1;
    while (i < 50) {
        i = 1 + i;
        if (i/10 == (i-1)/10) {
            print("i not divisible by 10, i is");
            printi(i);
            continue;
        }
        while (j < 40) { // only happens once, j is not reset
            j = j+1;
            if (j/7 != (j-1)/7) {
                print("j divisible by 7!");
                printi(j);
            } else if (j / 5 != (j-1)/5) {
                continue;
            }
            print("shouldn't print when j is divisible by 5 but not 7");
            print("j is");
            printi(j);
            continue;
            continue;
            continue;
        }
    }
}